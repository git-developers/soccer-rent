{% extends "AppBundle::template.html.twig" %}

{% block bodyclass %}

{% endblock %}

{% block stylesheets %}
    {{ parent() }}

    {#
    {% stylesheets
        '@AppBundle/Resources/public/common/css/tree.css'
    %}
    <link rel="stylesheet" href="{{ asset_url }}" />
    {% endstylesheets %}
    #}

    <style>

    </style>

{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {#
    {% javascripts
    '@AppBundle/Resources/public/tree/js/formCreate.js'
    %}
    <script src="{{ asset_url }}"></script>
    {% endjavascripts %}
    #}

    <script type="text/javascript">
        function  submitFunction() {

            alert('Compra con exito!');
            
        }
    </script>

{% endblock %}

{% block content %}

    <section class="content-header">
        <h1>
            <i class="fa fa-fw fa-cube"></i> Productos
            <small>list</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li class="active">Dashboard</li>
        </ol>
    </section>

    <section class="content">
        <form name="form-category-product">

            <div class="row">
                <section class="col-md-12 connectedSortable ui-sortable">
                    <div class="box box-primary">

                        {#
                        <div class="box-header ui-sortable-handle" style="cursor: move;">
                            <i class="ion ion-android-list"></i>
                            <h3 class="box-title">items</h3>
                            <div class="box-tools pull-right">
                               <button
                                        type="button"
                                        class="btn btn-default pull-right"
                                        alt="Crear item"
                                        title="Crear item"
                                        data-toggle="modal"
                                        data-target="#vv">
                                    <i class="fa fa-plus"></i> Crear item
                                </button>
                            </div>
                        </div>
                        #}

                        <div class="box-body">

                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>imagen</th>
                                        <th>producto</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>

                                    {% for key, has in categoryHasProduct %}

                                        {% set product = has['product'] %}

                                        <tr>
                                            <td>
                                                <img src="{{ product.image }}" alt="" class="img-thumbnail" style="max-height: 60px">
                                            </td>
                                            <td>{{ product.name }}</td>
                                            <td>
                                                <button
                                                    type="button"
                                                    class="btn bg-purple btn-xs"
                                                    data-toggle="modal"
                                                    data-target="#modal-credit-card">comprar</button>
                                            </td>
                                        </tr>

                                    {% endfor %}

                                </tbody>
                            </table>

                        </div>

                        {#
                        <div class="box-footer clearfix no-border">
                            <button type="button" class="btn btn-default pull-right"><i class="fa fa-plus"></i> Add item</button>
                        </div>
                        #}

                    </div>
                </section>
            </div>

        </form>
    </section>

    {{ include('AppBundle:Modal:credit_card.html.twig') }}

{% endblock %}